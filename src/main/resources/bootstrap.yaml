spring: 
  cloud:
    config:
      uri: ${currencyservice.configserverurl}
      fail-fast: false
  application:
    name: CurrencyConversionFactorService
  profiles:
    active: dev

server:
  port: 0 